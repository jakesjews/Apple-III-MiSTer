# Two 512-byte blocks: the ROM loads the first at $A000 and the program
# reads the second through the ROM's BLOCKIO.
MEMORY {
    BOOT: start = $A000, size = $400, fill = yes, fillval = $00, file = %O;
}
SEGMENTS {
    CODE: load = BOOT, type = ro;
}
