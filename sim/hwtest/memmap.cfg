# Three 512-byte blocks: the ROM loads the first at $A000 and the program
# reads the other two through the ROM's BLOCKIO.
MEMORY {
    BOOT: start = $A000, size = $600, fill = yes, fillval = $00, file = %O;
}
SEGMENTS {
    CODE: load = BOOT, type = ro;
}
